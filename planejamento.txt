get_next_line (fd, line)
    satatic char *save;
    char         *temp;

    get_line(fd, save, temp);   
        caso de errado retorna -1
        caso chegue ao fim, retorna 0
        caso leia certinho retorna 1

    zera o conteudo de save

    line = get_result(temp, save)
        aloca espaço e retorna a linha
        o que sobra vai para save

    free temp

    se o return_read for 0, ele libera a memoria

    retorna o return_read

get_line(fd, save, temp)
    int return_read;
    char buff;
    char line

    caso save esteja vazia
        aloca espaço para save
    caso save tenha conteudo
        recebe conteudo para dentro de line
    
    while (verifica se em line existe um "\n" e se o arquivo chegou a 0)
    {
        return_read = read(fd, buff, size)
        caso return_read seja 1 ou maior
            line = line + buff
    }

    libera buff
    temp = line
    return (return_read)

get_result(temp, save)
    char *line

    caso ouver "\n"
        copia o restante para save

    alloca memoria para line
    
    copia a linha para line

    retorna line;



    